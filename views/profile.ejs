<!DOCTYPE html>
<html lang="en">
<%- include('partials/header') -%>
<body>
<!--navigation-->
<header>
    <!-- Navbar -->
    <%- include('partials/navBar') -%>
    <!-- Navbar -->
    <!--Section: Design Block-->
    <section>
        <div id="intro" class="bg-image" style="
                background-image: url('https://www.columbiasouthern.edu/media/a3spk2gk/online-profile-laptop.jpg');
                height: 60vh;
                ">
            <div class="mask h-100" style="background-color: rgba(0, 0, 0, 0.6);">
                <div class="container d-flex justify-content-center align-items-center h-100">
                    <div class="row justify-content-center align-items-center">
                        <div class="col-lg-8 text-center p-3">
                            <h1 class="text-white text-center display-3">
                                Welcome <%= user.nickname %>
                            </h1>
                        </div>
                        <div class="col-lg-4 text-center p-3">
                            <% if (user.picture) { %>
                                <img class="block py-3" src="<%= user.picture %>" width="300" alt="img">
                            <% } %>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
    <!--Section: Design Block-->
</header>

<main>
    <div class="container my-5">
        <div class="row justify-content-center align-items-center">
            <div class="col-lg-10">
                <p class="py-3">
                    This is the content of <code class="bg-gray-200">req.user</code>.<br>
                    <strong>Note:</strong> <code class="bg-gray-200">_raw</code> and <code class="bg-gray-200">_json</code> properties have been omitted.
                </p>

                <pre class="block bg-gray-300 p-4 text-sm overflow-scroll"><%= userProfile %></pre>

            </div>
        </div>
    </div>
    <!-- Container for demo purpose -->
</main>

<%- include('partials/footer') -%>
<%- include('partials/scripts') -%>
</body>
</html>